# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
env/
.Python

# PyInstaller (executable build artifacts — 174MB, don't commit)
build/
dist/
*.manifest
*.spec.bak

# Flask app runtime
uploads/
outputs/
logs/
instance/
# 同事執行時自動從 dictionary.seed.db 種出來的累積學習版,不該入庫
dictionary.db

# Environment / secrets
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log

# Working DB backups (keep dictionary.seed.db which is the reference dictionary seed)
*.db.backup
*.db-journal

# docx 編輯暫存 (unpack/repack 中介)
.docx-work/
